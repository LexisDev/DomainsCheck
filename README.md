# Админ панель для автоматического мониторинга доступности доменов

Веб-приложение на Laravel для автоматического мониторинга доступности доменов с сохранением истории проверок и настраиваемыми параметрами.

## 📋 Описание проекта

Система позволяет пользователям:
- Регистрироваться и авторизовываться
- Добавлять домены для мониторинга
- Настраивать параметры проверок (интервал, таймаут, HTTP метод)
- Просматривать историю проверок с детальной информацией
- Автоматически получать результаты проверок по расписанию

## 🛠 Технологии

- **Backend:** Laravel 12+
- **PHP:** 8.2+ (рекомендуется 8.3+)
- **База данных:** MySQL 8+ / PostgreSQL 14+
- **Frontend:** Blade (Laravel Views)
- **HTTP клиент:** Guzzle (встроен в Laravel)

## ✨ Основной функционал

### Авторизация
- Регистрация пользователей
- Вход / Выход
- Защита приватных страниц через middleware

### Управление доменами
- Добавление / Редактирование / Удаление доменов
- Список доменов пользователя
- Настройка параметров проверки для каждого домена

### Настройки проверок
- **Интервал проверки:** от 1 до 1440 минут
- **Таймаут запроса:** от 1 до 60 секунд
- **Метод проверки:** HTTP HEAD или GET

### Автоматические проверки
- Проверки выполняются автоматически по расписанию (каждую минуту)
- Результаты сохраняются в историю
- Учитывается интервал проверки для каждого домена

### Логи проверок
- Дата и время проверки
- Результат (успех/ошибка)
- HTTP код ответа
- Время ответа в миллисекундах
- Сообщение об ошибке (если есть)

## 🚀 Установка

### Требования
- PHP 8.2+ (рекомендуется 8.3+)
- Composer
- MySQL 8+ или PostgreSQL 14+
- Расширения PHP: `pdo`, `mbstring`, `openssl`, `json`, `curl`

### Шаги установки

1. **Клонировать репозиторий:**
   ```bash
   git clone <repository-url>
   cd domains
   ```

2. **Установить зависимости:**
   ```bash
   composer install
   ```

3. **Настроить окружение:**
   ```bash
   cp .env.example .env
   php artisan key:generate
   ```

4. **Настроить базу данных в `.env`:**
   ```env
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=your_database
   DB_USERNAME=your_username
   DB_PASSWORD=your_password
   ```

5. **Выполнить миграции:**
   ```bash
   php artisan migrate
   ```

6. **Запустить сервер разработки:**
   ```bash
   php artisan serve
   ```

7. **Запустить планировщик задач (в отдельном терминале):**
   ```bash
   php artisan schedule:work
   ```

   Или настроить cron для продакшена (рекомендуемый вариант):
   ```bash
   * * * * * cd /path-to-your-project && php artisan schedule:run >> /dev/null 2>&1
   ```

## 📖 Использование

### Регистрация и вход
1. Перейдите на `/register` для создания аккаунта
2. Войдите через `/login`
3. После входа вы попадёте на Dashboard

### Добавление домена
1. Перейдите в "Мои домены" → "Добавить"
2. Заполните форму:
   - **Домен:** например, `example.com` или `https://example.com`
   - **Название:** опциональное имя для удобства
   - **Метод:** HEAD (быстрее) или GET
   - **Интервал:** как часто проверять (в минутах)
   - **Таймаут:** максимальное время ожидания ответа (в секундах)
   - **Активен:** включить/выключить мониторинг
3. Нажмите "Сохранить"

### Просмотр истории проверок
1. В списке доменов нажмите "История" у нужного домена
2. Просмотрите таблицу с результатами проверок
3. Фильтры и пагинация работают автоматически

### Настройка домена
1. Нажмите "Настроить" у нужного домена
2. Измените параметры проверки
3. Сохраните изменения

## 🏗 Архитектура

Проект следует принципам **SOLID** и лучшим практикам Laravel:

- **Тонкие контроллеры:** только маршрутизация и валидация
- **Сервисы:** вся бизнес-логика вынесена в сервисы
- **Form Requests:** валидация через отдельные классы
- **Policies:** авторизация через политики
- **Eloquent:** работа с БД через модели

### Структура проекта

```
app/
├── Console/
│   ├── Commands/
│   │   └── RunDomainChecksCommand.php    # Команда для проверки доменов
│   └── Kernel.php                        # Консольное ядро (загрузка команд)
├── Http/
│   ├── Controllers/
│   │   ├── Auth/                         # Контроллеры авторизации
│   │   └── Domain/                       # Контроллеры доменов
│   └── Requests/
│       ├── Auth/                         # Валидация авторизации
│       └── Domain/                       # Валидация доменов
├── Models/
│   ├── Domain.php                        # Модель домена
│   ├── DomainCheck.php                   # Модель проверки
│   └── User.php                          # Модель пользователя
├── Policies/
│   └── DomainPolicy.php                  # Политика доступа к доменам
└── Services/
    ├── Auth/
    │   └── AuthService.php               # Сервис авторизации
    ├── Domain/
    │   └── DomainService.php             # Сервис работы с доменами
    └── Monitoring/
        └── DomainCheckService.php        # Сервис проверки доменов
```

## 🔧 Команды Artisan

### Проверка доменов вручную
```bash
php artisan domains:check
```

Эта команда проверяет все активные домены, которые не были проверены в течение заданного интервала.

### Планировщик задач
```bash
php artisan schedule:work
```

Запускает планировщик. В Laravel 12 расписание определено в `routes/console.php`:
- `domains:check` запускается каждую минуту

Проверить, что задача зарегистрирована:

```bash
php artisan schedule:list
```

## 📊 База данных

### Таблицы

- **users** — пользователи системы
- **domains** — домены для мониторинга
- **domain_checks** — история проверок

### Связи

- `User` hasMany `Domain`
- `Domain` belongsTo `User`
- `Domain` hasMany `DomainCheck`
- `DomainCheck` belongsTo `Domain`

## 🌐 Развертывание

### На бесплатном PaaS (Railway / Render / Fly.io)

1. **Подготовьте репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. **Настройте переменные окружения:**
   - `APP_KEY` — сгенерируйте через `php artisan key:generate`
   - `DB_*` — настройки базы данных
   - `APP_URL` — URL вашего приложения

3. **Настройте build команды:**
   - `composer install --no-dev --optimize-autoloader`
   - `php artisan migrate --force`

4. **Настройте cron для планировщика:**
   - Добавьте задачу: `* * * * * php artisan schedule:run`

### Пример для Railway

1. Подключите GitHub репозиторий
2. Укажите build команду: `composer install --no-dev && php artisan migrate --force`
3. Настройте переменные окружения
4. Добавьте cron job для `schedule:run`

## ✅ Чек-лист проверки (Vagrant/Homestead)

```bash
cd /var/www/domains/domains

# версия
php artisan -V

# миграции
php artisan migrate

# ручной прогон проверок + запись в историю
php artisan domains:check

# планировщик
php artisan schedule:list
php artisan schedule:run -v
php artisan schedule:work
```

## 📝 Лицензия

MIT License

## 👤 Автор

Проект разработан в соответствии с техническим заданием.

## 🔗 Полезные ссылки

- [Laravel Documentation](https://laravel.com/docs)
- [Laravel Scheduling](https://laravel.com/docs/scheduling)
- [Guzzle HTTP Client](https://docs.guzzlephp.org/)
